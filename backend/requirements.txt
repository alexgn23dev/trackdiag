# Versiones fijadas (v0.5.71). Son exactamente las que se usaron para ejecutar
# y aprobar la batería de backend/tests/ y la validación de true peak.
#
# Por qué pineadas y no >=: las cinco librerías de audio determinan los
# NÚMEROS que ve el usuario. En particular `soxr` es quien calcula el
# sobremuestreo 4x del true peak: otra versión puede devolver otro valor sin
# que cambie una línea de nuestro código. Antes de v0.5.71 solo librosa estaba
# pineada, así que dos deploys del mismo commit podían medir distinto.
#
# Al subir cualquiera de las de audio: volver a pasar backend/tests/ y
# tests/validar_true_peak.py, y valorar si toca subir PEAK_ALGORITHM_VERSION o
# LOUDNESS_ALGORITHM_VERSION en engine/versiones.py.

# --- Web ---
fastapi==0.115.0
uvicorn==0.30.0
python-multipart==0.0.9
httpx==0.28.1
slowapi==0.1.10

# --- Audio (determinan las mediciones: no tocar a la ligera) ---
librosa==0.11.0
numpy==2.4.6
scipy==1.17.1
pyloudnorm==0.2.0
# soundfile trae libsndfile empaquetado en las wheels de manylinux; la versión
# de libsndfile queda determinada por esta línea, no por el `apt install
# libsndfile1` del Dockerfile (que se mantiene como red por si se instala
# desde fuente). Validado con libsndfile 1.2.2.
soundfile==0.14.0
soxr==1.1.0

# --- Auth ---
bcrypt==5.0.0
PyJWT==2.13.0

# --- Datos ---
asyncpg==0.31.0
alembic==1.19.0
psycopg2-binary==2.9.12

# --- Integraciones ---
resend==2.35.0
Pillow==12.3.0
